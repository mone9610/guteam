type User = {
  id: string;
  name: string;
  sub: string;
  introduction: string;
  // eslint-disable-next-line camelcase
  picture_url: string;
};
export type { User };

type Profile2 = {
  name: string;
  introduction: string;
  pictureUrl: string;
};
export type { Profile2 };
