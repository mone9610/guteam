version: 2

jobs:
  build:
    docker:
      - image: busybox
    steps:
      - run: echo "this is build task"  
  react:
    docker:
      - image: circleci/node:9.11
    working_directory: guteam/docker-codes/http-server/guteam/
    steps:
      - checkout
      - run: yarn build
      - run: aws s3 sync ./build s3://guteam-production --delete  

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - react:
          requires:
            - build
        
